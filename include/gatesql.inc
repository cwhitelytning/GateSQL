/**
 * Returns the index of the player in the table.
 * If there is no player online or he does not exist in the table, returns 0.
 * @param player_id
 * @return
 */
native gatesql_get_player_index(const player_id);